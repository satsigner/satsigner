{
  "name": "SatSigner snippets",
  "description": "Metadata file that tells IDEs from where to load project snippets",
  "engines": {
    "vscode": "^1.11.0"
  },
  "contributes": {
    "snippets": [
      {
        "language": [
          "typescriptreact"
        ],
        "path": "./typescriptreact.code-snippets"
      }
    ]
  }
}
