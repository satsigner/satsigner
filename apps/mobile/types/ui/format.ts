// Truncation size variants for the middle-truncating formatters in
// `@/utils/format`. Each maps to a head/tail character width per surface.

export type AddressSize = 'compact' | 'default'

export type TxIdSize = 'tiny' | 'compact' | 'default' | 'wide'

export type NpubSize = 'xs' | 'sm' | 'md' | 'lg' | 'xl'
