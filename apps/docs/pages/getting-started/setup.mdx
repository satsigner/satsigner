# Setup Local Development Environment

First, clone the `satsigner` repository

```bash
git clone https://github.com/satsigner/satsigner.git
```

Navigate to the root folder

```bash
cd satsigner
```

Install the dependencies

```bash
yarn install
```
