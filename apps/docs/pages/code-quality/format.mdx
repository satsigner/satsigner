# Format

At the root of the repository, you can run the prettier formatter with the following command:

```bash copy
yarn format
```

If you want it to try to fix warnings, please run:

```bash copy
yarn format:fix
```
